# RLN Data Schema

Visual entity-relationship reference for the RLN project's Brite schema: Dealers, Customers, Leads, Events, Quotes, Orders, and Work Orders, plus a combined master map and a ticket-level gap analysis for new tables/fields.

Open `index.html` in a browser, or view the live deployment.
